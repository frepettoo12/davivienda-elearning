rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Por ahora permitir todo (ajustar cuando agreguemos auth)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
